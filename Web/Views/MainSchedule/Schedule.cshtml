@model MainScheduleViewModel
@{
	ViewData["Title"] = "Schedule";
}
@*	<div class="container-fluid m-0 vh-100">
		<div class="row">
			<div class="col-1"></div>
			@foreach (Group group in Model.Groups)
			{
				<div class="col-2">
					<h6 class="text-center">@group.Course.Shortname-@group.Code</h6>
				</div>
			}
		</div>
		@foreach (string weekday in Model.Weekdays)
		{
		<div class="row container-fluid" style="height:50%">
			<div class="col-1">
				<h6 class="m-0 text-center" style="writing-mode:vertical-lr; text-orientation:upright; vertical-align:middle">@weekday</h6>
			</div>
			<div class="col-2 p-0">
				@for(int i = 1; i < 7; i++)
				{
					<div class="row border m-1" style="height:15.5%">
					
					</div>
				}
			</div>
		</div>
		}
		<hr class="w-100"/>
	</div>*@

<div>
		<div class="row flex-nowrap">
			<div class="col-1"></div>
			@foreach(Group group in Model.Groups)
			{
			<div class="col-2">
				<h6 class="text-center">@group.Course.Shortname-@group.Code</h6>
			</div>
			}
		</div>
		
	@foreach (string weekday in Model.Weekdays)
	{
		<div class="row flex-nowrap" style="height:550px">
			<div class="col-1" style="display:flex; align-items:center; justify-content:center;">
				<h6 class="m-0" style="width:1px; word-wrap:break-word; vertical-align:middle;">@weekday</h6>
			</div>
			@foreach (Group group in Model.Groups)
			{
				<div class="col-2 p-0">
					@for (int i = 1; i < 7; i++)
					{
						<div class="row border m-1" style="height:15.5%">
						</div>
					}
				</div>
			}
		</div>
		<hr class="w-100"/>
	}
</div>
